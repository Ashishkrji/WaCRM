import PlaceholderPage from "../_components/placeholder"
import { FileText } from "lucide-react"

export default function PromptsPage() {
  return <PlaceholderPage title="Prompt Manager" desc="Centralized library for system prompts, templates, and LLM instructions." icon={FileText} />
}
