"use client";

import PlaceholderPage from "../_components/placeholder"
import { Key } from "lucide-react"

export default function APIKeysPage() {
  return <PlaceholderPage title="API Keys" desc="Generate and manage API keys for external integrations and webhooks." icon={Key} />
}
