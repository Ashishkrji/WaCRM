"use client";

import PlaceholderPage from "../_components/placeholder"
import { UserCheck } from "lucide-react"

export default function EmployeesPage() {
  return <PlaceholderPage title="Employee Directory" desc="Manage employee profiles, departments, and reporting lines." icon={UserCheck} />
}
